export * from "./constants.core";

/**
 * Web version is square.
 *
 * Note: could do 25x24 instead of 25x25 so that it is the
 * same number of squares as the 20x30 mobile version,
 * but preferring to go with an exact square instead.
 */

/**
 * number of columns on the board
 */
export const WIDTH = 25;
/**
 * number of rows on the board
 */
export const HEIGHT = 25;